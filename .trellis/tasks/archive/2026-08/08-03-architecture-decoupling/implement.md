# 执行计划

- [x] 在 `core:playback` 定义持久化与内容来源能力接口，并替换具体 `core:data` 依赖。
- [x] 在 `app` 装配层实现无状态适配器，删除播放模块对 `core:data` 的 Gradle 依赖。
- [x] 引入类型化播放失败，替换 UI 的 `BAD_HTTP_STATUS` 字符串判断并增加测试。
- [x] 建立已登录业务编排器与窄 UI 依赖/动作契约，迁移退出和缓存清理流程。
- [x] 增加退出/鉴权失效顺序测试及模块边界结构检查。
- [x] 运行播放、数据、应用测试与 lint，再运行全量 `test lint --continue`。
- [x] 更新项目契约规范与任务验收记录。
