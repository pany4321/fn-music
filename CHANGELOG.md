# Changelog

## 1.3.8 - 2026-09-26

### 新增

- 设置页新增「界面缩放」：自动 / 标准（1.5 倍）/ 大（1.75 倍）/ 更大（2 倍）。「自动」按屏幕密度判定——1080P 车机（密度 ≤1.33）取 1.5 倍，电视与手机保持 1 倍；选中即时全局生效（文字、卡片、按钮、返回键、歌词字号一起缩放）并跨启动保存。
- 首页「随机漫游」卡片封面改为与其它卡片同一机制：启动时从曲库随机取 3 首歌曲的封面拼成叠层，运行期间保持不变，下次启动重新生成（请求失败重试一次，仍失败则退回专辑/歌单封面，卡片不会空着）。
- 放大显示（缩放 ≥1.4 倍）时封面自动请求更大的位图变体，避免小图被拉伸发虚。

### 修复

- 首页「歌单 / 随机专辑 / 随机歌曲 / 最近添加」等卡片此前没有卡片底色，封面与文字直接浮在页面背景上：现在统一带卡片底色 + 细描边，封面仍顶到卡片上沿（不缩小），文字行内缩 8dp。
- 目录卡片（专辑/歌曲/歌手/歌单网格）统一改用卡片 token：未聚焦为卡片底色 + 描边，聚焦提亮并显示主色描边。
- 修复主题 token 里的 6 位十六进制颜色被当成 alpha=0（全透明）导致卡片底色、卡片描边、面板底色、外框描边在六套主题下都画不出来的问题，补齐为 8 位 ARGB。

### 说明

- 卡片整体提亮，并新增「卡片描边」「外框描边」两个主题 token，六套主题各自派生；首页四张功能卡的渐变底色同步提亮。
- 界面缩放覆盖 `LocalDensity`（不改 `fontScale`），因此所有 dp/sp 尺寸等比缩放，无需逐个元素调整；设置页缩放档位为设备级偏好。

## 1.3.7 - 2026-09-26

### 修复

- 首页三张功能卡（随机漫游/收藏/最近播放）的图标描边统一改用**主题主色**，随主题切换一致变化（此前“最近播放”用警示色、六套主题取值相同而看不出变化；“随机漫游”用副色，与默认主题同值）。
- 六套主题的副色互不重复（石墨蓝的副色由青碧改为浅蓝），避免切换主题时个别元素看不出变化。

### 说明

- 界面配色现已全部收敛到 `Theme.kt`：UI 层（`ui/` 下除 `Theme.kt` 外）**不再存在任何颜色字面量**；胶囊、外框、卡片背景、面板、控件、对话框、播放页遮罩均取自可切换的主题 token，插画类图案（唱片/时钟/声波/心形/网格图块）取自集中定义的 `FnArt` 调色板。


## 1.3.6 - 2026-09-26

### 新增

- 设置页新增“主题”（位于“关于”之前）：内置六种配色（珊瑚夜/青碧/森野绿/紫罗兰/樱粉/石墨蓝），默认沿用原配色；选中后整个界面即时换色并持久化，启动首帧即为所选主题。
- 全局搜索新增“最近搜索”：搜索框下方最多两行胶囊（5 条 + “清空”共 6 格等宽，省略号截断，永不溢出），点击胶囊**立即搜索**（不填回输入框、不弹输入法、焦点留在胶囊上），点击结果时记录，右侧“清空”一键清除。

### 修复

- 搜索输入框会吞掉遥控器上下键，导致历史胶囊与结果区无法到达：输入框显式接管上下键（下→历史/结果，上→返回按钮）。
- 搜索页显示判定改用实际搜索词（此前用输入框文本，历史胶囊直搜时输入框为空会误显示“请输入关键词”）。
- 搜索历史焦点链补全：输入框 → 胶囊行 → 清空 → 结果区，胶囊聚焦有可见焦点框。
- 历史胶囊点击曾因 effect 把自己的 key 置空而自我取消、进而用空关键词清掉结果：拆成“输入防抖”与“历史直搜”两个独立 effect。
- 主题覆盖范围补完：**UI 层（app/…/ui 下除 Theme.kt 外的全部文件）不再出现任何颜色字面量**——胶囊、外框、卡片背景、面板、控件、对话框、播放页遮罩等全部改由主题 token（可随主题切换）或集中定义的插画调色板 `FnArt`（内容美术，不随主题变化）提供；默认主题取值与原样一致。修复“我的/首页标签胶囊颜色不随主题变化”。
- 历史胶囊点击改为**立即搜索**（不填回输入框、不弹输入法、焦点留在胶囊上）；点击结果才记录历史。
- 修复历史胶囊点击因 effect 自重置 key 而自我取消、进而用空关键词清空结果的问题（拆分为输入防抖与历史直搜两个独立 effect）。
- 设置页焦点顺序：缓存上限行向下不再跳过新增的主题行。
- “全部歌手/全部专辑/全部风格/全部歌单”等网格页：返回按钮纳入焦点链（网格首行向上即到返回按钮）。
- “我的”页从“全部风格/风格详情”返回后，焦点正确回到“全部风格”卡片（此前焦点恢复白名单缺少风格键）。
- 歌词当前行改为按背景对比度选色（方案 A）：主题强调色 → 强调色提亮/压暗 → 封面主色提亮/压暗 → 白/黑，取首个对比度 ≥ 4.5:1 的候选；逐字歌词已唱部分同色，切换仍无白闪与抖动。


## 1.3.5 - 2026-09-26

### 新增

- 设置页新增“主题”（位于“关于”之前）：内置六种配色（珊瑚夜/青碧/森野绿/紫罗兰/樱粉/石墨蓝），默认沿用原配色；选中后整个界面即时换色，无需重启。
- 全局搜索新增“最近搜索”：搜索框下方最多两行胶囊（每行三格，等宽省略号，永不溢出），点击可回填并重新搜索，右侧“清空”一键清除；点击搜索结果时才记录，避免存入半截关键词。

### 修复

- “全部歌手/全部专辑/全部风格/全部歌单”等网格页：返回按钮纳入焦点链（按下键从网格首行即可到达返回按钮，此前首行向上为 Cancel，遥控器无法聚焦返回按钮）。
- “我的”页从“全部风格/风格详情”返回后，焦点正确回到“全部风格”卡片（此前焦点恢复白名单缺少风格键，被判为无效而回落到标签）。
- 歌词当前行改为按背景对比度选色（方案 A）：依次尝试主题强调色 → 强调色提亮/压暗 → 封面主色提亮/压暗 → 白/黑，取第一个对比度 ≥ 4.5:1 的候选；逐字歌词的已唱部分同样使用该色，切换仍无白闪与抖动。


## 1.3.4 - 2026-09-26

### 修复

- 焦点死锁（首页/我的页）：列表由 LazyRow 改为常驻组合的横滚 Row。此前横向列表滚动后回收了行内条目，行级 FocusRequester 失效，焦点会卡在刷新按钮或底部行无法上下移动。
- 焦点死锁（同类的其余位置）：目录网格（全部歌手/全部专辑/全部风格/全部歌单）、艺人详情专辑网格、“我的”页四个 band 全部改为常驻组合；此前惰性回收后 itemFocuses/band requester 失效，会产生同类“焦点卡死无法移动”。
- 潜在崩溃加固：全局 15 处未保护的 `requestFocus()`（含播放失败重试后的动态返回焦点）统一加 `runCatching`；对未挂载的 FocusRequester 调用会抛 `IllegalStateException`。
- 焦点约定补全：网格每行的首列/末列左右键显式取消，不再跨行回绕；分页器在空页时上键取消。
- 首页歌单行：右键现在能走到最后的“全部歌单”卡片；行首按左不再逃逸到播放小窗口（左右键只在行内移动，行首/行尾钳制）。
- 首页四个行内左右键恢复正常移动（此前防逃逸钳制被错误地加到行内每一张卡片上）。
- 当前播放面板关闭按钮改用可触屏的 clickable 实现（此前 tv Button 在面板内不响应触摸）；点面板外仍可关闭。
- “我的”页：进入后焦点落在“我的”标签，按下键到搜索栏；搜索栏按左依次到“首页”标签、播放小窗口。
- 搜索页：按“下”正确落到第一个非空结果分区（此前初始焦点被三个分区同时挂载，跳过歌手列）。
- 歌词切换行不再白闪与抖动：去掉活动行的透明度/缩放过渡动画与字重变化，所有行颜色一致。
- 超长歌词行：前半段保持行首不滚动，播放进度过半后才开始向左平滑滚动，保证开头可读。
- 歌单/专辑/歌曲详情列表行：内容改为撑满行高后垂直居中（实测行中心 988px 与文字中心 985px 对齐）；此前内容按顶部对齐，比右侧居中的删除按钮高出约 11–14dp，看起来“删除按钮和本行没对齐”。行高 64dp、删除按钮 44dp。
- 删除歌曲失败时确认框保持打开并提示，不再静默失败。
- “全部歌手”“全部专辑”页新增返回按钮。
- “切换账号”改为应用内登录页（带返回按钮，触摸/遥控返回同样有效）：取消返回时保持原账号登录，不再直接登出。
- 登录页：提交时登录按钮保持可聚焦（此前禁用后焦点回落到 NAS 地址栏），仅显示“正在登录”并忽略重复点击。
- 播放页“播放模式”按钮移到“收藏”左侧，左移链（播放 → 上一首 → 添加到歌单 → 收藏 → 播放模式）与视觉顺序一致，漫游模式下同样包含“添加到歌单”。
- 随机专辑/随机歌曲的“刷新”按钮纳入显式焦点链，并在行内列表为空时指向下一个可聚焦行头，避免指向未挂载的焦点目标。


## 1.3.3 - 2026-09-25

### 修复

- 搜索页：按“下”正确聚焦到歌手列首项（此前初始焦点请求被同时挂到歌手/专辑/歌曲三个首项，后挂载者生效导致跳过歌手列）。
- 首页：歌单、随机专辑、随机歌曲、最近添加四行内左右键恢复正常移动（此前行尾防逃逸钳制被错误地施加到行内每一张卡上）；行尾防逃逸改为仅作用于各行的最后一张卡。
- 播放页：漫游模式下按左键不再跳过“添加到歌单”（该按钮在漫游下同样存在），无“上一首”时左键也落到“添加到歌单”。


## 1.3.2 - 2026-09-25

### 修复

- 稳定性：连续解码失败计数改为按真实播放进度（>1 秒）复位，不再在 STATE_READY 时复位——修复全队列格式不支持时自动跳转仍可能无限循环的问题。
- 稳定性：当前播放面板遮罩与面板底层改用 pointerInput 手势，不再成为 D-pad 可聚焦目标，消除“幽灵焦点”导致的遥控器失灵感。
- 稳定性：删除歌曲失败时对话框保持打开并显示“删除失败”提示（此前静默失败）。
- TV 易用性：“播放模式”按钮移至“收藏”左侧，播放页左移链（播放→上一首→添加到歌单→收藏→播放模式）与视觉方向完全一致。
- TV 易用性：“添加到歌单”对话框加载完成后自动聚焦第一个歌单。
- TV 易用性：搜索页无结果时按“下”、最近添加行在随机歌曲为空时按“上”，焦点显式取消，不再依赖未挂载 FocusRequester 的未定义行为。
- TV 易用性：随机专辑/随机歌曲的“刷新”按钮纳入显式焦点链（卡片上键 → 刷新 → 上一行）。
- TV 易用性：首页三张功能卡（漫游/收藏/最近播放）上键统一到“正在播放”（无媒体时到“首页”标签）。
- 车机易用性：歌单详情页删除按钮触控目标 40dp → 44dp，表头对齐占位同步更新。
- 清理仓库残留的全角形式 NAS 地址（示例 IP 已中性化）。


## 1.3.1 - 2026-09-25

### 修复

- 歌词从第一行（歌名/歌手/作词/作曲等元信息行）开始即按时间顺序依次居中显示：列表顶部留白固定为全屏视口的一半，不再随控制栏显隐变化——此前控制栏一出现，留白增大导致歌词内容被整体向下推。
- 歌词自动居中增加方向钳制：内容只随播放推进向上滚动，绝不向下滚动（手动滚动不受影响，向后跳转歌曲仍可重新定位）。
- 超长逐字歌词行（卡拉OK/QRC/YRC 格式）不再自动换行，与同步歌词行一致：单行显示，随演唱进度向左平滑滚动露出完整内容。


## 1.3.0 - 2026-09-25

### 修复

- 当前播放面板新增两个关闭入口：右上角关闭（✕）按钮（可聚焦、OK 触发），以及点击面板外的暗色区域直接关闭；关闭后焦点归还到打开面板的“播放队列”按钮。
- 歌词首行初始位置修正：歌词列表上下留白改为半个视口高度，第一行歌词从居中位置开始，不再顶死在上沿、播放第二句时才突然进入居中滚动。
- “添加到歌单”对话框弹出抖动修复：内容区与底部消息行改为固定高度，加载中/列表/提示之间切换时窗口尺寸不再跳变。


## 1.2.9 - 2026-09-25

### 变更

- 歌单详情页、收藏详情页、最近播放详情页的顶部封面改为直接复用首页对应卡片的封面：歌单用 3 首随机歌曲拼排（会话级缓存，与首页歌单卡片同源），收藏/最近播放用首页会话封面组，无拼排时回落服务器封面/默认样式。
- “全部专辑”卡片启用全新封面（层叠圆盘设计，用户从 C1–C4 候选中选定 C4）。


## 1.2.8 - 2026-09-25

### 修复

- 播放队列面板中点选歌曲开始播放后，自动关闭队列面板回到沉浸播放视图（此前保持打开）。
- 歌单删除确认框：取消/删除两按钮统一为同尺寸，文字在按钮内水平垂直居中。

### 变更

- “全部歌单”卡片封面改为动态拼排：每次启动时从曲库随机取 3 首歌曲的封面拼成，与其他歌单卡片同一套逻辑，运行期间保持不变。
- 风格卡片全新背景：采用均衡器渐变设计（B3 方案），应用于首页风格栏、全部风格网格，标题叠加左下角，选中态样式与其它卡片一致。


## 1.2.7 - 2026-09-25

### 修复

- 首页“随机专辑/随机歌曲”改为会话级缓存：仅启动时加载一次，切换页面/返回不再重复请求服务器，手动“刷新”按钮才重新采样。
- 播放页遥控器左移次序修正：播放/暂停 → 上一首 → 添加到歌单 → 收藏（此前跳过“添加到歌单”）；播放模式按钮移至收藏左侧仍可达。
- 歌单详情页表头“时长”列与数据行时间数值纵向对齐（此前与删除按钮对齐）。
- 删除确认框按钮横向均匀分布、文字居中。
- “我的”页全局搜索栏可通过遥控器上下键聚焦（此前被焦点链跳过），从顶部下移第一站即搜索栏。
- 登录页修复：HTTPS / 保持登录复选框遥控器 OK 可正常切换（焦点/点击语义重复导致此前无效）；登录提交后焦点不再跳回顶部输入框。
- 首页歌单/随机专辑/随机歌曲/最近添加四行，行尾继续按右键焦点停在本行最后一个元素，不再逃逸到右上角“我的”。


## 1.2.6 - 2026-09-25

### 修复

- “添加到歌单”对话框歌曲数量：batch-detail 接口按真实返回结构解包（此前解析失败被静默吞掉，数量永远显示 0）。
- 歌单卡片封面：修复封面拉取条件死锁（playlist/list 不返回数量导致拼排封面从未拉取），首页与全部歌单页现在显示歌曲封面拼排。
- 歌单详情删除按钮：重构为行按钮的兄弟节点，点击删除不再误触播放；删除前弹确认框，删除后列表原地更新、焦点落到相邻歌曲、同步失效服务端缓存。
- 最近播放页面：每次进入时强制刷新最新播放记录。
- 搜索页：按方向键下焦点显式落到第一个搜索结果，不会卡在输入框。


## 1.2.5 - 2026-09-25

### 修复

- 首页“最近添加”行：遥控器方向键向下现在可以到达（之前焦点被拦截，只能触屏操作）。
- 搜索页：输入关键词后按方向键下可直接进入搜索结果（之前焦点无法离开输入框）。
- 播放页“添加到歌单”：每个歌单显示真实的歌曲数量（改用批量详情接口），添加成功/失败提示改为应用内文字。
- 歌单详情页删除歌曲：按钮有清晰的焦点样式，删除前需确认；删除后列表原地更新，不再跳回第一页，焦点自动落到相邻歌曲。
- 播放容错：连续 3 首歌曲解码失败后停止自动跳转，避免整队列格式不支持时无限循环空转。
- 弹窗关闭后焦点显式归位，避免遥控器短暂“失灵”。
- 搜索防抖状态修复，快速连续输入不再出现永久“正在搜索…”。

### 变更

- 车机场景“返回键后台运行”默认开启：播放页按返回键进入后台继续播放，可在设置中关闭。
- 搜索结果达到单页上限（20 首）时显示“可换更精确的关键词”提示。
- 搜索空态提示改为“按确认键呼出键盘输入”。
- README 重写为规范的上游归属声明（基于 QiaoKes/fn-music-tv，本分支由大南瓜维护）。


## 1.2.4 - 2026-09-25

### 优化

- 歌词时间同步优化：当前演唱行的时间判定与显示精确匹配。
- 当前演唱行始终保持在歌词区域垂直居中位置，上下滚动采用平滑动画。
- 超长歌词行不再自动换行，随演唱进度自动向左平滑滚动，完整展示歌词内容。
- 当前演唱行以加粗与珊瑚色高亮显示，与其它行形成鲜明对比。


## 1.2.3 - 2026-09-25

### 新增

- 全局搜索上线：我的页搜索框支持歌手、专辑、歌曲名称匹配，结果分三类展示；点击歌手或专辑自动播放其歌曲，点击歌曲即播放该搜索结果队列。
- 首页新增“最近添加”歌单行，展示最近入库的 16 首歌曲。
- 我的页新增“风格”分类与全部风格浏览，可按风格筛选并播放歌曲。
- 全部歌手/全部专辑/全部风格/全部歌单页新增返回按钮。
- 播放页“添加到歌单”在漫游模式下同样可用。

### 变更

- 歌单卡片封面改为随机取歌单内最多三首歌曲的封面拼排，各歌单封面每次启动随机变化。
- 首页卡片封面（漫游/收藏/最近播放）每次启动随机采样，页面切换不再重复变化。
- 随机专辑/随机歌曲仅在启动时加载一次，切换页面不再重复请求；“刷新”按钮可手动重摇。
- 歌单详情页每首歌曲新增“删除”按钮，可将歌曲从歌单中移除。
- 设置页新增返回按钮；CD/海报模式选中态样式统一，“大海报模式”更名为“海报模式”。
- 更新作者与仓库信息。


## 1.2.2 - 2026-09-24

### 新增

- 首页新增“最近添加”歌单行，展示最近入库的 16 首歌曲，与“随机歌曲”风格一致。
- “我的”页面新增搜索入口与“风格”分类：搜索歌手、专辑、歌曲三类结果；风格列表与歌手/专辑一致，可进入风格详情页播放。
- “全部歌单”页新增返回按钮。

### 修复

- 修复首页“我的”标签无法进入的问题。
- 歌单卡片封面现在随机取歌单内最多三首歌曲的封面，不再固定取前三首。
- 播放页“添加到歌单”按钮在漫游模式下也可用，且“播放模式”按钮焦点链同步修正。
- “随机专辑”“随机歌曲”的刷新按钮尺寸缩小并与标题文字对齐。

### 调整

- 登录地址容错增强：显式填写 http:// 或 https:// 的完整地址不再被追加默认端口。
- 设置页新增返回按钮，作者与仓库信息更新。


## 1.2.1 - 2026-09-24

### 修复

- 补齐方向盘/耳机媒体键支持：声明媒体按键接收器，应用进程被系统回收后按播放键也能拉起服务并恢复上次播放队列。
- 播放中方向盘的播放/暂停、上一首、下一首均由系统媒体会话直接驱动；上一首在播放超过 3 秒时先重播当前歌曲，3 秒内切换到上一首。


## 1.2.0 - 2026-09-24

### 新增

- 首页新增“最近播放”功能卡与最近播放列表页，与收藏采用相同的封面拼排与队列逻辑。
- 首页新增“随机专辑”与“随机歌曲”区块，各显示 16 项并支持“刷新”重新随机。
- 播放页新增左上角返回按钮与“添加到歌单”按钮，可直接把当前歌曲加入已有歌单。
- 设置页新增“车载”分组与“返回键后台运行”开关，车机上返回键退到后台而不退出应用。
- 歌单卡片改为展示歌单内前三首歌曲的封面拼排，不再使用服务端生成的歌单封面。

### 车机适配

- 横屏布局按屏幕宽度自适应边距与网格列数，超宽或矮屏车机不再裁切内容。
- 根布局统一处理安全区，适配车机挖孔屏与常驻系统栏。
- 触控目标统一提升到 48dp：设置控件、队列删除、分页箭头、导航标签、详情按钮。

### 修复

- 修复“我的”标签在首页无法进入的问题。
- 修复 HTTPS 无法登录的问题：裸地址或 IP 开启 HTTPS 时默认使用 5667 端口，并对 IP 直连放宽证书主机名校验（域名仍严格校验）。
- 修复网络中断后播放无法恢复的问题：新增连接重探测与播放失败重试，队列中的旧地址会自动改写。
- 修复服务器地址混入全角符号（如 `192。168。1。10`）时无提示且无法登录的问题。
- 修复媒体按键在进程重启后无法继续播放的问题（熄火后用播放键可恢复上次队列）。
- 修复部分车机熄屏或降频时播放卡顿的问题（播放器保持本地唤醒锁）。
- 修复 Android 13 及以上系统不申请通知权限导致播放通知不显示的问题。
- 修复历史歌单缓存目录符号链接可能被遍历删除的问题。

### 品牌

- 应用名称更新为“飞牛音乐”，并启用新的唱片声波图标与电视横幅。


## 1.1.2 - 2026-09-03

### 修复

- 修复登录页输入局域网 IPv4、IPv6 或域名及自定义端口时，端口被实时清除的问题；

## 1.1.1 - 2026-08-31

### 修复

- 修复部分小米电视及旧版 Android 电视播放 FLAC 歌曲无声的问题。

## 1.1.0 - 2026-08-25

### 兼容性

- 应用最低系统要求降至 Android 6.0，重新支持更多旧款电视与投影设备。
- 完善旧版 Android 系统上的应用内更新兼容性，确保更新包校验和安装引导能够正常使用。

### 体验优化

- 歌单详情切换为新版布局，并与收藏、全部歌曲和专辑详情保持一致。

### 修复

- 修复播放器进度条获得焦点时出现大块白条、两端异常变长的问题。
- 修复收藏详情页重复显示“收藏”标题的问题。

## 1.0.8 - 2026-08-24

本版本为 1.0.7 的重新发行版本，包含 1.0.7 的全部功能与体验改动。

### 安装与更新

- 正式 APK 恢复 v1+v2 双签名，修复部分海信 Vidda 设备从文件管理器点击 APK 没有反应、旧版本无法覆盖安装的问题；现在无需先卸载旧版本。
- 重构应用内更新的安装流程。

### 体验优化

- 重新设计首页与“我的”页面：使用电视端胶囊分段导航、账号资料条和更统一的横向媒体卡。
- 首页“随机漫游”和“收藏”改为统一的大幅功能卡，优先展示真实媒体封面；无图条目不再进入封面组合。
- “我的”页面统一歌手、专辑与全部歌曲入口，歌手保持圆形头像，专辑保持方形封面，长名称自动换行或省略。
- 全部歌手与全部专辑改为每页 12 项的固定 4×3 网格，底部使用上一页、页码和下一页分页器，无需纵向滚动。
- 优化封面内存与磁盘缓存衔接，已加载图片可立即复用，并在后台异步更新高规格封面，减少页面返回和启动后的占位闪烁。

### 修复

- 修复横向媒体列表到达“全部歌手”或“全部专辑”后继续按右键会跳到顶部控件，以及离开列表后无法稳定返回末项的问题。
- 修复部分列表首尾焦点描边被裁切、遥控器向下无法进入下一内容区，以及加载完成后需要再按一次遥控器才显示封面的问题。

## 1.0.7 - 2026-08-24

### 体验优化

- 重新设计首页与“我的”页面：使用电视端胶囊分段导航、账号资料条和更统一的横向媒体卡，精简重复标题与常驻品牌文字。
- 首页“随机漫游”和“收藏”改为统一的大幅功能卡，优先展示真实媒体封面；无图条目不再进入封面组合。
- “我的”页面统一歌手、专辑与全部歌曲入口，歌手保持圆形头像，专辑保持方形封面，长名称自动换行或省略。
- 全部歌手与全部专辑改为每页 12 项的固定 4×3 网格，底部使用上一页、页码和下一页分页器，无需纵向滚动。
- 优化封面内存与磁盘缓存衔接，已加载图片可立即复用，并在后台异步更新高规格封面，减少页面返回和启动后的占位闪烁。
- 启动品牌过渡加入应用 Logo；进入首页后不再重复显示左上角“回声台”。

### 修复

- 修复横向媒体列表到达“全部歌手”或“全部专辑”后继续按右键会跳到顶部控件，以及离开列表后无法稳定返回末项的问题。
- 修复全部歌手与全部专辑分页仍按旧的 50 条页大小判断，导致第 7 页之后无法继续翻页的问题。
- 修复部分列表首尾焦点描边被裁切、遥控器向下无法进入下一内容区，以及加载完成后需要再按一次遥控器才显示封面的问题。

## 1.0.6 - 2026-08-12

### 安装提示

由于新增了安装权限，部分严格设备不允许覆盖安装，如果从旧版本覆盖安装时点击 APK 没有反应，请先卸载旧版本，再安装 v1.0.6。

卸载会清除本地保存的登录信息与应用设置，安装后需要重新登录。

### 新增

- 支持检查应用更新，并在应用内完成更新下载与安装流程。

## 1.0.5 - 2026-08-10

### 新增

- 歌词解析与展示统一迁移到 Accompanist Lyrics，支持网易云 YRC、QQ 音乐 QRC、酷狗音乐 KRC 逐字歌词，并为三个来源保留普通 LRC 回退。
- 在线歌词可同时保留原文、翻译与逐字时间。

### 体验优化

- 在线歌词优先选择带翻译的结果，其次选择可靠的逐字歌词，再按时长差与“网易云 > QQ 音乐 > 酷狗音乐”的来源顺序确定结果。
- 逐字歌词仅在歌曲时长差不超过 2 秒时启用；普通逐行歌词允许 5 秒内的误差，超出逐字范围的候选会安全降级为逐行展示。
- 播放器改为多行平滑滚动歌词，增大翻译字号并扩大控制栏隐藏时的歌词区域，同时隐藏罗马音、日文括号注音与无意义的过场圆点。

### 修复

- 修复设置页“在线歌词匹配”无法通过电视遥控器确认键切换的问题，并保持触摸操作一次只触发一次。
- 修复切歌或歌词资源更新后列表沿用旧滚动位置，导致歌词内容、当前行或滚动进度对不上的问题。
- 修复旧歌词匹配缓存继续沿用过期排序和时长规则的问题；升级缓存协议后会自动重新匹配。

## 1.0.4 - 2026-08-09

### 体验优化

- 播放器歌词改为仅展示当前歌词与下一句，移除占用空间较多的罗马音，并重新调整原文、翻译和下一句之间的字号、明暗与间距。
- 海报模式与 CD 模式统一采用稳定的双区域排版，长句最多显示两行，在 720p 和 1080p 电视画面下保持清晰且不重叠。
- 逐字歌词在播放期间按屏幕刷新节奏平滑推进，暂停、跳转和切歌时会及时校准到实际播放位置。

### 修复

- 修复逐字歌词直接跟随 250 毫秒播放进度刷新而产生阶梯式卡顿的问题。
- 修复多语歌词同时展示前一句、当前句、下一句及罗马音时内容拥挤、视觉层级混乱的问题。

## 1.0.3 - 2026-08-08

### 体验优化

- 在线歌词会等待 QQ 音乐、酷狗音乐和网易云音乐全部完成检索，再按“逐字歌词 > 带翻译歌词 > 普通在线歌词”的内容丰富度选择最优结果；仅当所有在线来源都没有可用歌词时才回退到飞牛音乐歌词。
- 移除在线歌词匹配的 3 秒总超时和首个结果后的提前截止逻辑，同时保留每个来源独立的网络超时，避免单个来源长期阻塞。
- 播放器将原文、翻译和罗马音分行呈现，逐字歌词随播放进度高亮；前一行、当前行和后一行使用固定高度槽位，在海报与唱片样式下保持稳定排版。

### 修复

- 修复 `RTRT` 等歌曲可能被较早返回的普通歌词抢占，未选中网易云逐字双语歌词的问题。
- 修复行级 LRC 被错误合成逐字高亮的问题，并升级歌词匹配缓存协议，使旧的低质量匹配结果自动失效并重新选择。
- 修复连续歌词时间戳较密时，切换动画保留多个旧内容而造成歌词叠字的问题。

## 1.0.2 - 2026-08-08

### 新增

- 新增独立的在线歌词匹配模块，并发检索 QQ 音乐、酷狗音乐和网易云音乐，通过标题、歌手、专辑、时长及版本标签筛选高置信度结果。
- 播放器默认展示匹配歌词的原文与译文；在线匹配没有结果、来源异常或解析失败时自动回退到飞牛音乐歌词。
- 在线匹配结果按账号和歌曲持久缓存，重复播放无需再次搜索；短期无结果也会缓存，避免连续请求。
- 设置页新增“在线歌词匹配”开关，默认开启；关闭后直接使用飞牛音乐歌词。

### 性能与体验优化

- 移除响应较慢的 LRCLIB 来源，仅保留三个低延迟来源，并采用并发搜索、分来源超时和总时限控制，避免歌词匹配阻塞播放体验。
- 完善歌曲元数据补全后的歌词重试、过期缓存失效和账号切换逻辑，并保留各账号的播放器及在线歌词设置。

## 1.0.1 - 2026-08-06

### 新增

- 登录历史支持按服务器和账号保存多个配置，选择账号后直接登录，并支持单条删除与清空全部。
- 保持登录会加密持久化账号、密码 SHA-256 摘要、安全码和 token，不保存明文密码。

### 修复

- 修复投影仪启动时网络尚未就绪会掉到登录页的问题；应用现在会保留凭据并在进程内自动重试。
- 修复播放器控制栏隐藏时，遥控器中键可能连续触发暂停和播放的问题；现在每次按键只切换一次播放状态。

## 1.0.0 - 2026-08-03

### 新增

- 新增跨设备同步的歌曲收藏：播放器可直接点亮红心，首页可进入独立收藏列表，收藏状态以 NAS 服务端为准。
- 播放队列新增逐项删除，支持删除当前歌曲、队尾歌曲及最后一首，并保持播放状态、随机顺序和焦点正确迁移。

### 体验优化

- 首页调整为“随机漫游 / 收藏”首行与歌单次行，重新设计随机漫游、收藏和全部歌单的入口封面。
- 播放器收藏按钮使用更清晰的心形，已收藏状态在获得焦点后仍保持红色。
- 右侧播放队列改为更紧凑的行式布局，当前歌曲、焦点和删除操作更易识别。
- 收藏详情复用首页收藏封面；无封面歌手统一显示圆形首字头像，歌曲、歌单和专辑使用现代首字封面，不再显示旧唱片占位图。

### 修复

- 修复收藏写入失败后可能保留错误本地状态的问题，失败时会回退到服务端已确认状态。
- 修复队列删除后焦点悬空、重复歌曲删错位置以及分页数据重新插入已删除项的问题。

## 0.1.13 - 2026-08-03

### 性能

- 将播放进度与低频播放状态拆分，非播放器页面不再随进度刷新重复重组，纯进度更新也不再重建完整播放队列。
- 将播放快照编码移出主线程，并优化图片缓存、数据库预算治理和会话恢复路径，减少切歌、缓存写入与应用启动时的阻塞。
- 动态详情页退出后会释放对应页面状态，同时保留返回栈内的分页、选中项与焦点恢复能力。
- 为应用补充 Baseline Profile，改善 Android TV 上的启动和关键操作性能。

### 重构

- 按页面与领域职责拆分超大 UI 和播放控制文件，移除无调用的旧实现及无用依赖，保持现有业务逻辑、界面布局和控制语义不变。
- 将 UI、会话/缓存/播放编排和数据访问解耦，使用明确的状态、动作与能力接口收敛模块依赖。

### 修复

- 统一歌曲点击、队列选择、上一首、下一首和自动切歌的呈现逻辑；新封面与歌词加载期间保留上一首已就绪内容，避免无图或无歌词的中间状态。
- 避免播放列表安装和元数据补全重复重启封面、歌词请求，缩短下一首内容出现前的等待时间。
- 修复播放列表使用方向键连续下移或删除焦点行时的滚动抽搐、焦点回跳问题。
- 修复 Android 6.0（API 23）上的请求头兼容性问题。

## 0.1.11 - 2026-08-02

### 新增

- 支持通过触屏点击导航、音乐卡片、列表、网格、设置项和播放器控件，同时保留电视遥控器焦点与 D-pad 操作。
- 支持使用 HTTP、HTTPS 或 FNID 连接 NAS；FNID 会自动探测局域网、外网和飞牛中继地址。
- 支持可选的飞牛访问码验证，并将访问凭据应用于登录、音乐接口、封面和音频请求。

### 修复

- 修复未填写端口的 HTTPS 域名连接，默认使用 443 端口；未填写协议的传统 HTTP 地址仍兼容 5666 端口。
- 修复部分电视按钮不响应触屏点击的问题，并避免滑动列表时误触发卡片点击。
- 设置页可在较小的横屏设备上滚动，并扩大播放器触屏热区，不影响现有播放和随机漫游逻辑。
- 优化 1080p 登录页显示，并为 FNID 无可用连接、缺少访问码和访问码错误提供明确提示。
